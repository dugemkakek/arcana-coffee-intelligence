{
  "name": "@arcana/ai-adapters",
  "version": "0.1.0",
  "private": true,
  "description": "LLM provider interfaces + MiniMax, Anthropic, OpenRouter, local adapters.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist", "prompts"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests yet'",
    "lint": "echo 'no lint yet'",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@arcana/shared-types": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  }
}
